version: '3.9'
services:
  app:
    build: .
    container_name: classic-furniture-backend
    ports:
      - "5001:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: npm run dev
    restart: unless-stopped

volumes:
  node_modules:
